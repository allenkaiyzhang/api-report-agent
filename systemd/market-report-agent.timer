[Unit]
Description=Market Report Agent Timer — run intraday reports on schedule
Requires=market-report-agent.service

[Timer]
# Check every minute; the scheduler inside decides whether to run
OnCalendar=*:0/1
Persistent=true
AccuracySec=5s

[Install]
WantedBy=timers.target
